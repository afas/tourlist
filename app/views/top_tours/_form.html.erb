<%= form_for(@top_tour) do |f| %>
  <% if @top_tour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@top_tour.errors.count, "error") %> prohibited this top_tour from being saved:</h2>

      <ul>
      <% @top_tour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :bronni_id %><br />
    <%= f.text_field :bronni_id %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
